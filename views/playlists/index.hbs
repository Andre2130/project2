    {{#each playlists}}
        <div>
            {{this.name}} - {{this.country}}
            <a href="/playlists/{{this._id}}">{{this.name}} Info</a>
            <a href="/playlists/{{this._id}}/songs">{{this.name}}'s Songs</a>
        </div>
    {{/each}}
 <div>
        <a href="/playlists/new">Add New playlists</a>
    </div>