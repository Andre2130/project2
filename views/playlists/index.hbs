   <h1>Sauce Radio</h1>
   
   
   <nav class="main">
    {{#each playlists}}
        <div >
            <button id="playlists"><a href="/playlists/{{this._id}}" role="button">{{this.name}}</a></button>
        </div>
    {{/each}}
   </nav>

 <div>
        <a href="/playlists/new">Add New playlists</a>
    </div>
    </div>